import {DefaultTheme} from "@react-navigation/native";


export const CustomNavigationTheme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '#316FCC',
    },
};
